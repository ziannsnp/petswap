# Comma-separated exact origins, without trailing slashes.
SIGN_IN_ALLOWED_ORIGINS=https://petswap-web.zianporrutai.workers.dev,http://localhost:5173,http://127.0.0.1:5173
# Optional: permits HTTPS preview hosts with an alphanumeric/hyphen prefix.
# Leave empty to disable preview origins.
SIGN_IN_PREVIEW_HOSTNAME_SUFFIX=-petswap-web.zianporrutai.workers.dev
